# frisk

## Examples:

```
# Success, exit code 0
docker run --rm phill97/frisk:latest -url https://qgr.se

# Fail (404), exit code not 0
docker run --rm phill97/frisk:latest -url https://qgr.se/u3ssV1I7JB6lwpvl
```
